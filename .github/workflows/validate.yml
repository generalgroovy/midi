name: Validate

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Validate Python
        run: python -m py_compile traktor-system-controller.py
      - name: Validate JSON
        run: python -m json.tool config.example.json >/dev/null
      - name: Validate shell
        run: bash -n install.sh
